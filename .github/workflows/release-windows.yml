name: Publicación antigua de Lulu Finity (deshabilitada)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Este workflow 0.7 quedó deshabilitado. Usa el workflow versionado más reciente."
