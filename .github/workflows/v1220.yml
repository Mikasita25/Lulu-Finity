name: Lulu Finity 1.2.2

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "build-v1220/**"
      - ".github/workflows/v1220.yml"

permissions:
  contents: write

env:
  BASE_URL: https://github.com/Mikasita25/Lulu-Finity/releases/download/v1.2.1/Lulu-Finity-Source-1.2.1.zip
  BASE_SHA256: b93f88bff849ea23259cc7d22357092a7863d1aee01a5709cd2d81e135e48901

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Reconstruir y parchear 1.2.2
        run: |
          set -euo pipefail
          curl --fail --location --silent --show-error "$BASE_URL" -o base.zip
          echo "$BASE_SHA256  base.zip" | sha256sum --check
          unzip -q base.zip -d app
          python build-v1220/apply-v1220.py app
          python build-v1220/test-v1220.py app
      - name: Validar código y pruebas
        run: |
          set -euo pipefail
          node --check app/src/main.js
          node --check app/src/preload.js
          node --check app/src/renderer.js
          node --check app/src/music-recovery-policy.js
          cd app
          npm ci --ignore-scripts --no-audit --no-fund
          npm test

  windows:
    needs: validate
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Reconstruir fuente 1.2.2
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $true
          Invoke-WebRequest -Uri $env:BASE_URL -OutFile base.zip
          if ((Get-FileHash base.zip -Algorithm SHA256).Hash.ToLower() -ne $env:BASE_SHA256) { throw 'Fuente 1.2.1 incorrecta' }
          Expand-Archive base.zip -DestinationPath app -Force
          python build-v1220/apply-v1220.py app
          python build-v1220/test-v1220.py app
      - name: Crear fuente reproducible
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          New-Item -ItemType Directory source-v1220 -Force | Out-Null
          foreach ($file in @('package.json','package-lock.json','NOTICE.md','README.md','CHANGELOG.md','.gitignore')) {
            if (Test-Path "app/$file") { Copy-Item "app/$file" "source-v1220/$file" -Force }
          }
          foreach ($folder in @('src','resources','build','railway-relay')) {
            if (Test-Path "app/$folder") { Copy-Item "app/$folder" "source-v1220/$folder" -Recurse -Force }
          }
          Compress-Archive -Path 'source-v1220/*' -DestinationPath 'Lulu-Finity-Source-1.2.2.zip' -Force
      - name: Probar y compilar Windows
        working-directory: app
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
        run: |
          npm ci --no-audit --no-fund
          npm test
          npm run build:win -- --publish never
      - name: Probar arranque real
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $profile = Join-Path $env:RUNNER_TEMP 'lulu-v122-profile'
          $startupMarker = Join-Path $env:RUNNER_TEMP 'lulu-v122-startup.txt'
          $navigationMarker = Join-Path $env:RUNNER_TEMP 'lulu-v122-navigation.json'
          New-Item -ItemType Directory -Path $profile -Force | Out-Null
          Remove-Item $startupMarker,$navigationMarker -Force -ErrorAction SilentlyContinue
          $env:LULU_STARTUP_SMOKE_MARKER = $startupMarker
          $env:LULU_NAVIGATION_SMOKE_MARKER = $navigationMarker
          $executable = Resolve-Path 'app/dist/win-unpacked/Lulu Finity.exe'
          $process = Start-Process -FilePath $executable -ArgumentList "--user-data-dir=$profile" -PassThru
          try {
            $ready = $false
            for ($attempt = 0; $attempt -lt 120; $attempt++) {
              Start-Sleep -Milliseconds 500
              $process.Refresh()
              if ($process.HasExited) { throw "Lulu Finity se cerró durante la prueba con código $($process.ExitCode)" }
              if ((Test-Path $startupMarker) -and (Test-Path $navigationMarker)) { $ready = $true; break }
            }
            if (!$ready) { throw 'La interfaz no confirmó el arranque completo' }
            if ((Get-Content $startupMarker -Raw).Trim() -ne 'ready') { throw 'Marca de arranque inválida' }
            $navigation = Get-Content $navigationMarker -Raw | ConvertFrom-Json
            if (!$navigation.ok -or @($navigation.results).Count -lt 12) { throw 'La prueba de navegación no pasó' }
          }
          finally {
            Remove-Item Env:LULU_STARTUP_SMOKE_MARKER -ErrorAction SilentlyContinue
            Remove-Item Env:LULU_NAVIGATION_SMOKE_MARKER -ErrorAction SilentlyContinue
            Stop-Process -Name 'Lulu Finity' -Force -ErrorAction SilentlyContinue
          }
      - name: Verificar artefactos del actualizador
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $installer = 'app/dist/Lulu-Finity-Setup-1.2.2.exe'
          $zip = 'app/dist/Lulu-Finity-1.2.2-x64.zip'
          $latest = 'app/dist/latest.yml'
          $blockmap = 'app/dist/Lulu-Finity-Setup-1.2.2.exe.blockmap'
          $source = 'Lulu-Finity-Source-1.2.2.zip'
          foreach ($file in @($installer,$zip,$latest,$blockmap,$source)) {
            if (!(Test-Path $file)) { throw "No se generó $file" }
          }
          $latestText = Get-Content $latest -Raw
          if (!$latestText.Contains('version: 1.2.2')) { throw 'latest.yml no anuncia 1.2.2' }
          if (!$latestText.Contains('Lulu-Finity-Setup-1.2.2.exe')) { throw 'latest.yml no apunta al instalador 1.2.2' }
          Expand-Archive $zip -DestinationPath verify-v122 -Force
          $asar = Get-ChildItem verify-v122 -Recurse -Filter app.asar | Select-Object -First 1
          if (!$asar) { throw 'Falta app.asar' }
          $asarText = [Text.Encoding]::UTF8.GetString([IO.File]::ReadAllBytes($asar.FullName))
          foreach ($token in @('v1.2.2','automation-action-volume-input','automation-action-volume','lastPayloadAt','youtubeAdMuteFailsafeTimer')) {
            if (!$asarText.Contains($token)) { throw "El paquete no contiene $token" }
          }
          $files = @($installer,$zip,$latest,$blockmap,$source)
          $lines = foreach ($file in $files) { "{0}  {1}" -f (Get-FileHash $file -Algorithm SHA256).Hash.ToLower(), (Split-Path $file -Leaf) }
          $lines | Set-Content SHA256SUMS.txt -Encoding utf8
      - name: Publicar Lulu Finity 1.2.2
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = 'Stop'
          $files = @(
            'app/dist/Lulu-Finity-Setup-1.2.2.exe',
            'app/dist/Lulu-Finity-1.2.2-x64.zip',
            'app/dist/latest.yml',
            'app/dist/Lulu-Finity-Setup-1.2.2.exe.blockmap',
            'Lulu-Finity-Source-1.2.2.zip',
            'SHA256SUMS.txt'
          )
          gh release view v1.2.2 *> $null
          if ($LASTEXITCODE -eq 0) {
            gh release upload v1.2.2 $files --clobber
            gh release edit v1.2.2 --title 'Lulu Finity 1.2.2' --notes-file 'build-v1220/release-notes.md' --latest
          } else {
            gh release create v1.2.2 $files --target $env:GITHUB_SHA --title 'Lulu Finity 1.2.2' --notes-file 'build-v1220/release-notes.md' --latest
          }
      - uses: actions/upload-artifact@v4
        with:
          name: Lulu-Finity-Windows-v1.2.2
          path: |
            app/dist/Lulu-Finity-Setup-1.2.2.exe
            app/dist/Lulu-Finity-1.2.2-x64.zip
            app/dist/latest.yml
            app/dist/Lulu-Finity-Setup-1.2.2.exe.blockmap
            Lulu-Finity-Source-1.2.2.zip
            SHA256SUMS.txt
          if-no-files-found: error
          retention-days: 30
